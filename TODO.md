cmd/inspect/image:
- Extract any errors!!!!!

cmd/todo:
- manage per-customer, per-opp to-do lists, with templates for the various products

cmd/process:
- checklist of various processes
- explanation of individual steps when prompted

cmd/docs:
- collection of iot read-the-docs
- search function
- AI?? Maybe connect to NotebookLM instance (maybe cmd/notes instead)

lib/common.sh:
- add function for calling applications contained within the snap

cmd/run:
- run commands within the snap directly

"core" component:
- add a component full of applications you'd only really need if installing the snap on a Core device.

misc:
- is there a way to add AI skills? maybe a skill for formatted BSP analysis, so that HW tables can be automagically generated??
