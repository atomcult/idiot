#!/bin/sh

print_description() {
	echo "Change directory to this application's data dir"
}

main() {
	set -eu
	case "${1:-}" in
		description) print_description ;;
		*)
			printf 'cd "%s"' "$IDIOT_DATA_ROOT"
			;;
	esac
}

main "$@"
