#!/usr/bin/env sh
# cmd/store/help - usage for the store subcommand group

. "$(dirname "$0")/../../lib/common.sh"

say "Usage: idiot store <subcommand>"
echo >&2
say "Subcommands:"
say "  add       Save a store"
say "  remove    Remove a saved store"
say "  list      List saved stores"
say "  use       Activate a store"
say "  status    Show the active store"
say "  unset     Clear the active store"
echo >&2
say "Environment:"
say "  IDIOT_STORES_DIR    Stores directory (default: $IDIOT_STORES_DIR)"
