#!/usr/bin/env sh
# cmd/store/help - usage for the store subcommand group

. "$(dirname "$0")/../../lib/common.sh"

say "Usage: idiot store <subcommand>"
echo >&2
say "Subcommands:"
say "  add <name> <store-id>    Save a Dedicated Snap Store ID under a human-readable name"
say "  remove <name>            Remove a saved store"
say "  list                     List saved stores and their IDs"
say "  use <name>               Set UBUNTU_STORE_ID to the named store's ID"
say "  unset                    Unset UBUNTU_STORE_ID"
echo >&2
say "Environment:"
say "  IDIOT_STORES_DIR    Stores directory (default: $IDIOT_STORES_DIR)"
