#!/usr/bin/env sh
# cmd/models/help - usage for the models subcommand group

. "$(dirname "$0")/../../lib/common.sh"

say "Usage: idiot models <subcommand>"
echo >&2
say "Subcommands:"
say "  pick      Interactively select a model assertion"
say "  update    Fetch the latest model assertions"
echo >&2
say "Environment:"
say "  IDIOT_MODELS_DIR    Models repository"
