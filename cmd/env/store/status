#!/usr/bin/env sh
# cmd/env/store/status - show the current store ID
# Usage: idiot env store status

. "$(dirname "${0}")/../../../lib/common.sh"

main() {
    if [ -n "${UBUNTU_STORE_ID:-}" ]; then
        say "${BOLD}${UBUNTU_STORE_ID}${RESET}"
    else
        say "${DIM}global${RESET}"
    fi
}
main "$@"
